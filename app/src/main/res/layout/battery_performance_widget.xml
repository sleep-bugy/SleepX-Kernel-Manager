<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="horizontal"
    android:padding="8dp"
    android:background="@drawable/widget_background"
    style="?android:attr/buttonBarStyle">

    <Button
        android:id="@+id/battery_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginEnd="4dp"
        android:text="@string/widget_battery_off"
        android:textSize="12sp"
        android:textStyle="bold"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:background="@drawable/widget_button_disabled"
        android:clickable="true"
        android:focusable="true"
        android:drawableTop="@drawable/ic_battery_alert"
        android:drawablePadding="4dp"
        android:gravity="center"
        style="?android:attr/buttonBarButtonStyle" />

    <Button
        android:id="@+id/performance_button"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="4dp"
        android:text="@string/performance_mode_balanced"
        android:textSize="12sp"
        android:textStyle="bold"
        android:paddingTop="8dp"
        android:paddingBottom="8dp"
        android:background="@drawable/widget_button_balanced"
        android:clickable="true"
        android:focusable="true"
        android:drawableTop="@drawable/ic_speed"
        android:drawablePadding="4dp"
        android:gravity="center"
        style="?android:attr/buttonBarButtonStyle" />

</LinearLayout>
